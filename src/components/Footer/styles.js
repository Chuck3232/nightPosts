import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  container: {
    position: 'absolute',
    textAlign: 'right',
    marginTop: '10px',
    fontSize: "15px",
    color: 'white',
  },
 
}));